# TODO

Running checklist for the CalFresh Data Dashboard rebuild. See README.md for the why behind each item. Update this as work progresses — check items off, don't delete them, so the history of what's been done stays visible.

## Phase 1 — Research & design
- [x] Identify the three CDSS data sources and their actual download URLs (dashboard page links directly to a single always-current Excel file; CF296/CF18 are one file per fiscal year)
- [x] Inspect real file structure (sheets, columns, header patterns, de-identification markers) using sample files
- [x] Decide data layer: Google Sheets + Apps Script (not Cowork-native, not all-Vercel)
- [x] Decide frontend: static site on Vercel (not a Cowork artifact)
- [x] Decide schema scope: mirror all columns from all three sources (not a curated subset) — confirmed this fits at ~40% of Sheets' cell cap
- [x] Design retention policy: rolling 25-year window + 80%-of-cap safety-valve auto-prune

## Phase 2 — Data pipeline (Google Apps Script)
- [x] Set up a new Google Sheet to serve as the data layer ("CalFresh Data - Consolidated", in the djconsults.com `CalFresh Data Dashboard Reboot` folder)
- [x] Write Apps Script functions to fetch the CDSS Dashboard page and CF296/CF18 listing pages and discover the current file links (not hardcoded, so new fiscal years are picked up automatically) — see `pipeline/SourceDiscovery.gs`
- [x] Write "has this changed since last run?" detection (compares the "Updated" date text on each listing page against stored state, no wasted downloads)
- [x] Write xlsx-to-Sheet conversion + tab-per-source ingestion — `pipeline/IngestMaster.gs`, `pipeline/IngestCF.gs`
- [x] Build the "Cell N" → real-name label mapping for CF296 (135 cells, reconstructed from merged header rows) and CF18 (68 cells, from its DataDictionary tab) — `pipeline/Labels.gs`
- [x] Write historical backfill, self-continuing across Apps Script's 6-minute execution cap — `runInitialBackfill()` in `pipeline/Main.gs`
- [x] Implement the retention/pruning logic and its Pipeline Log tab — `pipeline/Retention.gs`
- [ ] **Not yet done:** actually paste the code into the Sheet's Apps Script editor and run it (see `pipeline/SETUP.md` — this is a hands-on step only Diana can do)
- [ ] Handle "*" de-identified cells: currently carried through as literal text (not coerced to a number or blank) — worth a first-run check that this doesn't cause type issues in Sheets formulas the frontend might rely on
- [ ] Publish the Sheet to the web (read-only) for the frontend to consume (`SETUP.md` step 7 — sharing-permission change, only Diana can do this)
- [x] **Live-test `SourceDiscovery.gs` against the real page HTML** — done 2026-07-16; needed one real fix (whitespace-encoding bug, see README Key Decisions Log), now confirmed correctly discovering all three sources
- [x] **Live-test the narrowed OAuth scopes** (`appsscript.json`, added 2026-07-16) — confirmed the consent screen shows the narrower wording and `testDiscovery` runs cleanly under it
- [x] Run `runInitialBackfill()` — first run (2026-07-16) got dashboard master + all 6 CF18 years, but only 1 of 10 CF296 years, due to CF296 format drift across the years (see README Key Decisions Log). `IngestCF.gs` rewritten with a variant-detection system to handle all 3 known CF296 formats plus a new `CF296_Legacy` tab for the pre-2020 era.
- [x] **Re-run `runInitialBackfill()`** — all 10 CF296 years and all 6 CF18 years now confirmed present. Two transient Google-side errors along the way resolved on manual retry; added automatic retry for that error class so this shouldn't need a human next time.
- [ ] Spot-check the resulting data against the source Excel files, including the new `CF296_Legacy` tab
- [x] Install the weekly trigger (`installWeeklyTrigger`) — confirmed running
- [x] Publish the Sheet to the web (`SETUP.md` step 8) — confirmed working via the `gviz/tq?tqx=out:csv&sheet=NAME` endpoint, which lets the frontend fetch by tab name rather than needing opaque gid numbers

**Phase 2/3 complete.** The data pipeline is fully live and self-sufficient: weekly checks, automatic backfill/retry, retention enforcement, public CSV read access. Nothing further needed here unless CDSS changes format again.

## Phase 3 — Automation
- [x] Install the time-driven trigger in Diana's Google account — confirmed running (weekly, given CDSS updates roughly every 1–3 weeks)
- [x] Confirm the trigger survives independent of any Cowork session — by design (Apps Script triggers run server-side under Diana's Google account, not tied to any Cowork session)

## Phase 4 — Frontend (Vercel)
- [x] **First working prototype built 2026-07-29**: `index.html` recreates the "ParticipationTrend" tab (Total Participants area chart, Annual % Change multi-county comparison, county-size-bucketed bar charts with per-group averages, SSI/COVID-19/emergency-allotment reference lines) using real data extracted directly from the actual downloaded Master file (all 58 counties + Statewide, 2014–2026, 150 months). Built with Chart.js + a small custom reference-line plugin (no external annotation-plugin dependency). This is a feasibility test, not final styling — colors are explicit placeholders pending the color-for-meaning conversation. **Real finding worth flagging to Diana:** the ~2016–2017 data gap visible in the old Tableau screenshot does *not* appear in the current Master file's CalFresh Persons series for San Francisco — CDSS may have backfilled/fixed it since, or it was specific to a different tab/metric. Needs Diana's read on this before assuming our pipeline is missing something.
- [ ] Production version needs to swap the prototype's embedded static JSON snapshot for a live `fetch()` against the published Google Sheet's CSV export at runtime (the embedded-snapshot approach was just to get a working prototype quickly without needing live Sheet access from this environment).
- [ ] **Explore what's newly available in CDSS's data since ~2018** (Diana, 2026-07-29): the Master dashboard file has grown a lot since her original Tableau workbook was built — worth a real pass comparing the current ~303-column Master_Monthly schema (plus the Point-in-Time/Quarterly/Annual/PRI tabs) against what existed circa 2018, to find new metrics that didn't exist before and might be worth adding as new views, not just recreating the old ones. Not started yet.
- [ ] **Design fidelity guidance from Diana (2026-07-29):** recreate the *chart type choices* from the old Tableau workbook (she spent a lot of time experimenting on those originally, and stands by them), but not the visual styling — those were constrained by what Tableau offered, and there's real room for a more modern look and feel. **Color usage specifically needs its own dedicated design conversation before we build** — Diana cares a lot about using color meaningfully (not just decoratively), so don't default to arbitrary palette choices; revisit this explicitly once we're in the visual design phase.
- [ ] Look at Diana's old Tableau Public vizzes together to identify which specific views/filters to recreate. First tab reviewed 2026-07-29 via screenshot: "CalFresh Participants" (Total Participants area chart for a single selected county + a separate Annual % Change line-comparison chart with its own multi-select county filter, plus a bottom bar chart of annual % change by county grouped by County Size with an average reference line per group). More tabs to come (Applications & Outcomes, Discontinuances, Churn, Demographics, PRI) — Diana to send remaining screenshots before we start building, so shared chart/filter components can be designed once across all tabs rather than per-tab.
- [ ] **Deliberately keep the two top-of-tab charts on separate filters, not unified**: the single-county "Total Participants" trend and the multi-county "Annual % Change" comparison chart serve different purposes (raw scale vs. relative-growth context) and putting a small county like SF on the same axis as statewide totals would flatten it to invisible — confirmed with Diana 2026-07-29 there's no obviously cleaner unification, so keep them as two distinct filter widgets like the original.
- [ ] **Reference lines/annotations for time-series charts** (confirmed with Diana 2026-07-29): mark June 1, 2019 (SSI/SSP cash-out reversal — SSI/SSP recipients became CalFresh-eligible for the first time; sourced from ACL 22-85, see `DATA_DICTIONARY.md`), March 2020 (COVID-19 pandemic onset), and February/March 2023 (COVID-19 emergency allotments phased out) on any chart showing participation trends over time, so viewers have context for major shifts rather than reading them as organic growth/decline.
- [ ] **Data gap in the original "Total Participants" chart (~late 2016–early 2017) is a real CDSS reporting gap**, not a Tableau extract artifact (confirmed with Diana 2026-07-29) — recreate as an actual visual gap/break rather than interpolating across it, if our pipelined data has the same gap.
- [x] **Full tab inventory reviewed 2026-07-29** via 14 screenshots total, all archived as actual reference images (not just descriptions) in `/tableau-reference/` at the project root, with a manifest mapping each file to its tab — see `tableau-reference/README.md`. Covers: ParticipationTrend, Summary_Page1, Summary_Page2, Summary_Page3 (impact-calculator page), PRI (includes a CA county choropleth map), Dual Enrollment, Application Outcomes (standalone), DaysToApproval, Processing Compliance, Churn Apps History, Churn Recert Outcomes, plus two zoomed-in impact-estimator tooltip screenshots. **Confirmed by Diana as complete** — the "Definitions & Sources" tab's content (region/county-size definitions) is already captured elsewhere in this project, no further extraction needed.
- [ ] **Consortia is no longer a relevant grouping/filter** (confirmed with Diana 2026-07-29) — all counties have consolidated onto one statewide system, CalSAWS, replacing the old C-IV/CalWIN/LRS three-way split entirely (see `DATA_DICTIONARY.md`'s Consortium section, now marked historical). Drop "Consortia" from the planned filter list; the old dashboard's Consortia-based churn-chart grouping (used because CalWIN/C-IV seemed to calculate some churn metrics differently) no longer applies.
  - **Open question raised by Diana 2026-07-29, not yet decided**: how far back should churn-related historical charts go, given consortia used to be "a real driver of differences" but no longer matters? Two options: (a) only show churn history from whenever CalSAWS consolidation completed (cleaner, but the rollout was staggered per-county over roughly 2021–2024 rather than one single date, so this would need real per-county migration-date research before it could be precise); (b) show full history but add a caveat note (similar in spirit to the PRI limitations note) that pre-consolidation churn data may reflect differing calculation methods across the old C-IV/CalWIN/LRS systems, without needing an exact per-county cutoff. Leaning toward (b) as simpler and equally honest, but this needs Diana's confirmation before building.
- [ ] **The old Tableau's churn charts used a different/older CF18 report format than what we pipeline now** (confirmed with Diana 2026-07-29) — the chart *designs* (stacked bars, recertification outcome breakdowns, applications-from-churn) are a good visual reference, but the underlying metrics/columns won't map 1:1 to our current 68-cell CF18 schema. Needs a real re-mapping pass against current `Labels.gs` CF18 labels before rebuilding any churn view, not an assumption of equivalence.
- [x] **Scope decision: rebuild the "impact calculator" tables — confirmed in scope** (Diana, 2026-07-29). The three formulas (Applications → Case-Months, Approval Rate → Case-Months, Recertification Churn → Case-Months) are now fully documented in `DATA_DICTIONARY.md`'s new "Impact Estimator Methodology" section, sourced from Diana's own original tooltip text. Still to do: decide the reference-anchor date/window for the "average" and "best" calculations (the original used "since July 2016" — keep a fixed historical anchor, use a rolling window, or anchor to CalSAWS consolidation? see the Consortia note above) before implementing.
- [x] **Attribution — context resolved, wording still pending** (Diana, 2026-07-29): Diana was herself the consultant on the original CDSS/Sacramento State "Alliance to Transform CalFresh" (ATC) project these chart designs and the PRI 80%-by-2019 target came from — this isn't really "borrowing someone else's work," it's her own earlier professional work. ATC itself no longer exists. Decision for now: keep a note that the PRI target (and any other ATC-era benchmarks) were goals set "back then," and that today's advocates might want to keep using them as-is or set updated ones. Still need final exact wording/placement in the UI.
- [x] **New chart type identified: California county choropleth map** (PRI tab) — needs county boundary geometry and a mapping approach (e.g., a CA counties GeoJSON/TopoJSON + a lightweight mapping library), a different technical dependency than anything else in this project so far. **Demand not yet confirmed** (Diana, 2026-07-29): unclear how much current usage/interest there is in PRI specifically, or how often it'd even be updated. Keep on the list, but validate actual interest before building — worth pairing with the "request form" idea in Ideas/future considerations below, or just asking directly, before investing in the mapping dependency.
- [ ] Build the static HTML/JS dashboard: county + date-range + category filter controls, plus (per Diana, 2026-07-17): caseload size, region, and year(s) as additional filters, and ABAWD waiver county status as a candidate filter to investigate
- [ ] **County Size filter** (from Diana's old Tableau definitions, 2026-07-17): X-Large = 6 largest counties, Large = >25,000 households, Medium = 5,000–25,000 households, Small = <5,000 households. Originally based on DFA256 household counts, but DFA256 appears retired (CDSS's Report Form page shows it effective only "November 2003 through April 2024," replaced by CF256) — recommend deriving this instead from Master_Monthly's `CalFresh Households` column, which we already pipeline and is current. Needs confirming this produces reasonably similar county groupings before treating it as equivalent. **Decided 2026-07-29:** use the 4-bucket XL/L/M/S scheme (not the simpler 3-bucket Large/Medium/Small scheme seen in the first old Tableau screenshot reviewed). X-Large's "6 largest counties" will be computed programmatically each refresh (ranked by current `CalFresh Households`) rather than hardcoded, so it stays accurate as caseloads shift — **flagged as provisional, needs a final confirm-or-adjust pass once we can see real ranked output.**
- [ ] **Region filter** (from Diana's old Tableau definitions, 2026-07-17), verbatim county groupings:
  - Bay Area: Alameda, Contra Costa, Marin, Napa, San Francisco, San Mateo, Santa Clara, Solano, Sonoma
  - Central Coast: Monterey, San Benito, San Luis Obispo, Santa Barbara, Santa Cruz, Ventura
  - Central Sierra: Alpine, Amador, Calaveras, Inyo, Mariposa, Mono, Tuolumne
  - Inland Empire: Riverside, San Bernardino
  - Los Angeles: Los Angeles
  - Northern: Butte, Colusa, Del Norte, Glenn, Humboldt, Lake, Lassen, Mendocino, Modoc, Nevada, Plumas, Shasta, Siskiyou, Sierra, Tehama, Trinity
  - Other Southern CA: Imperial, Orange, San Diego
  - Sacramento Area: El Dorado, Placer, Sacramento, Sutter, Yolo, Yuba
  - San Joaquin Valley: Fresno, Kern, Kings, Madera, Merced, San Joaquin, Stanislaus, Tulare
- [x] **Standing rule: vertical axes always start at zero** (Diana, 2026-07-31) — applies to every chart in this project, count-based or percentage-based (Chart.js `min: 0` for always-positive metrics, `beginAtZero: true` for metrics that can go negative like annual % change, so the true zero point is never cropped out of view). Applied 2026-07-31 to all y-axes in the participation view (`index.html`) — keep applying to every new chart going forward by default, no need to ask again.
- [ ] **Accessibility is a first-class design requirement, not a later pass** (per Diana, 2026-07-17 — redoing this after the fact would be a shame): WCAG-compliant color contrast, keyboard navigation, ARIA labels/roles on all interactive filter controls, and a non-chart fallback (e.g. a data table) for anything rendered as a canvas/SVG chart, since screen readers can't parse those directly
- [ ] Every view/chart should link back to its original CDSS source file/page, for transparency and so advocates can verify a number themselves
- [ ] Beyond just a source link, give users a way to look up the report source and whatever definitions we have for the specific metrics/columns shown in each graph (per Diana, 2026-07-28) — e.g. an info icon or expandable panel on each chart pulling the relevant entry from `DATA_DICTIONARY.md`, rather than making users go find the doc themselves. Needs a design decision: inline tooltip vs. a linked panel vs. something else.
- [ ] Recreate the topic areas from the original Tableau vizzes: Applications & Outcomes, Discontinuances (CF296), Churn (CF18), Participation & Demographics, Program Reach (PRI)
- [ ] Wire up live fetch from the published Sheet (`gviz/tq?tqx=out:csv&sheet=NAME` endpoint, confirmed working 2026-07-16)
- [ ] Deploy via Diana's existing GitHub → Vercel workflow (she designs in Cursor, hosts as a free Vercel Hobby project) — hand off as repo-ready files, not a zip/drag-and-drop package

## Phase 4a — Participation view: UX & content polish (Diana, 2026-08-13)

**Filter & layout UX**
- [x] **Moved each chart's filters onto its own card — 2026-08-14.** County selector now lives in the Total Participants card, Compare-counties in the Annual % Change card, Region/Size/County/Clear-all in the bucket chart card. New lightweight `.card-filters` CSS class (flex row, no extra background/border — the `.card` it sits inside already carries the link-a/link-b tint) replaces the old standalone `.filters` blocks grouped separately at the top. The shared date-range slider stays where it was, since it genuinely applies to both top charts at once rather than belonging to one.
- [x] **Tooltip hover fixed — 2026-08-14.** Root cause: every line dataset uses `pointRadius: 0`, and Chart.js's default hover settings (`mode: 'nearest', intersect: true`) require the cursor to land on the exact pixel of an (invisible) point. Added `interaction: { mode: 'nearest', intersect: false, axis: 'x' }` to all three charts — tooltip now fires from anywhere near a line's x-position.
- [x] **"Clear all filters" button added — 2026-08-14.** New button in the bucket chart's card-filters, deselects every option in Region/Size/County and rebuilds the chart.
- [x] **Month picker added for the bucket chart — 2026-08-14.** Previously hardcoded to `DATA.latest_complete_month` via a single-month pre-bucketed snapshot (`DATA.bucket_bar_data`). Replaced with a `countyRowsForMonth(month)` function that builds the same `{county, yoy, size}` rows for any month from data already on the page (`DATA.series[county].yoy_pct[month]` + `DATA.county_meta[county].size`), plus a `<select>` populated with every month that has a real statewide YoY figure (135 months, 2015-01 through the latest), newest first. `DATA.bucket_bar_data` is no longer read but left in the DATA blob since stripping it doesn't meaningfully shrink the page. Bonus: this automatically excludes Feb 2019 and Feb 2020 from the picker, since Statewide's YoY for those months is null after the Feb-2019-data-gap fix above — the broken months were never offerable in the first place.
- [x] **Cross-chart filter auto-sync — 2026-08-14.** The countySelect → compareSelect direction was already covered by the existing `setDefaultCompare()`. Added the missing direction: compareSelect's change handler now checks whether the county driving Total Participants got deselected, and if so switches that chart to the first remaining selected county (or Statewide, matching `currentCompare()`'s own empty-selection fallback).
- [x] **"CalFresh dollars issued" dual-axis overlay — built 2026-08-14** (Diana). Landed as another toggle in the existing Participant Types control ("$ Issued (right axis)"), not a stacked-layout change and not a separate dashboard, per Diana's read that side-by-side already works fine for the two top charts. Source: `Master_Monthly`'s `Total Issuances` column (Diana confirmed the exact name, monthly cadence), no new Apps Script needed since it's a plain existing column — Diana exported it directly from `Master_Monthly` as CSV (`County`, `Month`, `Calendar Year`, `Total Issuances`) and uploaded it, same channel as the Student data.
  - Implementation: `buildTotalChart()` in `index.html` now supports a second Chart.js axis (`y1`) via an `axis: 'y1'` field on the overlay's `DATA.demographic_layers.overlays` entry — the persons-axis rescale-when-total-hidden logic explicitly excludes any `yAxisID: 'y1'` dataset so a dollar figure (hundreds of millions) can never blow out the persons axis. New `fmtDollarsAbbrev()`/`fmtDollarsFull()` helpers handle $K/M/B abbreviation for axis ticks and first/last point labels, and full-precision `$#,###` for tooltips and the accessible data table.
  - **Real coverage gap, confirmed via the export:** the Statewide `Total Issuances` figure goes all the way back to Jan 2014 (matching everything else on this chart), but **county-level breakdowns only exist from Sep 2023 onward** — every one of the 58 counties starts exactly at 2023-09 with no exceptions, a hard cutoff rather than a gradual rollout. For now the chart just shows a gap before Sep 2023 for any county other than Statewide, same honest-gap treatment as everything else nulled/missing on this chart. **Fix for this gap: see the DFA256/CF256 pipeline item directly below.**

- [x] **Region comparison added to the Annual % Change chart — 2026-08-14.** Diana: "I'm finding myself wanting a view of the % change over time where I can see region differences without having to know off-hand which counties are in which region." Added a new "Compare regions" multi-select (alongside the existing Compare-counties select) populated from the existing `REGION_ORDER`/`COUNTY_REGIONS` mapping (already used by the bucket chart). Each selected region draws one dashed, population-weighted aggregate line via a new `computeAggregateSeries()` helper (same weighting math as the bucket chart's group-average lines, just run across the full month range instead of one snapshot month), styled with a fixed per-region color so a given region reads the same color every time it's picked. (Also tried an "All counties" sentinel option on the county select itself — Diana tried it and asked to remove it 2026-08-14, "it doesn't do what I want." Removed.)
  - **"Drill into region" button added — 2026-08-14.** Diana's actual want: pick a region and see just that region's member counties plus Statewide, to trace an odd-looking regional aggregate back to the county causing it. New button next to Compare-regions, enabled only when exactly one region is selected (drilling into >1 region at once is ambiguous). Click sets Compare-counties to that region's members + Statewide and re-renders; the region's dashed aggregate line stays on the chart since Compare-regions itself is untouched.
  - **Leader-line clutter fixed — 2026-08-14.** Diana, after trying the drill-in on a bigger region: "the leader lines get a little nuts once there are a bunch of counties." The shared `firstLastLabelPlugin` (draws the value number at each line's first/last point, used on both top charts) now skips drawing any first/last labels once more than 6 lines are visible at once -- legend and hover tooltip still identify every line, the numeric labels were a convenience for a handful of lines, not a requirement. Also fixed a latent bug while in there: the plugin previously labeled every `_firstLast` dataset regardless of whether it was toggled off in the legend; it now respects `chart.isDatasetVisible()`, so clicking lines off in the legend is also a way to drop back under the 6-line threshold and get labels back for what's left.

- [ ] **Pick up later: multi-select filter boxes (Compare counties, Compare regions, Region, County Size, County) are still rendering much wider than their content needs.** Flagged by Diana 2026-08-14 via screenshot — "these filters are much wider than they need to be" — and still not fixed as of 2026-08-14 ("it didn't fix it, but I don't have the patience right now"). Two attempts so far, neither worked: (1) added `width: 200px` directly on `.filter-group.multi select` (previously only had `min-width: 200px`, no cap) — no visible change; (2) also wrapped the containing `.filter-group.multi` div in `width: 200px; overflow: hidden` as a hard clip, on the theory that some browsers size a `<select multiple>` listbox to its widest option regardless of an explicit `width` on the select itself — still no visible change per Diana's follow-up screenshot. Both CSS changes are still in the file (didn't hurt anything, just didn't fix it). Worth investigating with the actual browser DevTools open next time (inspect the computed width and box model on the live `<select>` element) rather than guessing blind — there may be a rule or default I'm not seeing from the source alone, or this may need a genuinely different approach (e.g. rebuilding the multi-selects as a custom checkbox-list component instead of native `<select multiple>`, which would also sidestep this whole class of native-control quirk).

- [ ] **Pick up later: pipeline the DFA 256 / CF 256 report to fill the pre-Sep-2023 county-level `$ Issued` gap above.** Scoped 2026-08-14, deliberately not started yet — Diana chose to finish the rest of this Phase 4a list first and treat this as its own push afterward, since it's real Phase 2 (pipeline) work, not a quick fix.
  - **What it is:** "Food Stamp Program Participation and Benefit Issuance Report" — CDSS's DFA256, renamed CF256 as of May 2024 (same report, name changed, not a different report). Diana confirmed monthly county-level data exists this way back to at least Fiscal Year 2015-16 (oldest period listed on CDSS's report-period page) and shared 3 real files as proof: `DFA256FY22-23.xlsx`, `DFA256FY23-24.xlsx`, `CF256 May 2024 and Ongoing.xlsx`.
  - **This is the exact same report family/format as CF296/CF18** — confirmed by inspecting all 3 files directly: same `Release Summary` / `DataDictionary` / `Data_External` (or `Data_Internal` in the May-2024-onward file — a real format variant, the same kind of drift `IngestCF.gs`'s variant-detection system already handles for CF296) sheet structure, same `Cell N` → Part/Item/Column dictionary pattern, same one-row-per-county-per-month data layout (`Date`, `County Name`, `County Code`, `SFY`, `FFY`, then Cell-numbered data columns). This means the existing pipeline architecture (`IngestCF.gs` + `Labels.gs`'s cell-mapping approach) can very plausibly be extended to a third report type rather than built from scratch.
  - **The dollar field itself, located in the FY23-24 DataDictionary:** Part "C. VALUE OF BENEFIT ISSUANCES DURING THE MONTH", Item "20. Total" (Cell 30 in that year's numbering — cell numbers may shift between format variants, same caveat as CF296's `CF296_LABELS`/`CF296_Legacy` split). This is almost certainly the direct equivalent of `Master_Monthly`'s `Total Issuances` field, just sourced from a different, longer-running CDSS report.
  - **Concrete build steps, comparable in size to the original CF296/CF18 addition:** (1) `SourceDiscovery.gs` support for the DFA256/CF256 report-period listing page (screenshot on file from Diana showing the real page layout — 10 fiscal-year links plus "May 2024 and Ongoing"); (2) a new or extended `Labels.gs` cell mapping built from the real `DataDictionary` sheets (only the dollar-issuance section has been inspected so far, not the full ~30-cell dictionary); (3) an ingestion path in `IngestCF.gs` (or a new file) handling the `Data_External`/`Data_Internal` variant split; (4) historical backfill across ~11 fiscal years; (5) only then wiring the resulting pre-Sep-2023 county-level dollar figures into `index.html`'s `dollars_issued` overlay series (same `series[county][period] = value` shape already used there, so this last step should be quick once the data exists).
  - The 3 sample files Diana uploaded are in this session's uploads, not saved anywhere in this project folder yet — grab fresh copies from CDSS's report-period page when starting this rather than relying on stale local copies.

**Content/education**
- [x] **Plain-language jargon definitions added — 2026-08-14:**
  - [x] Annual % change tooltip wording updated to "The [percentage change in total participants/statewide percentage change in total participants/aggregate or average percentage change in total participants] between DATE and DATE was XX.X%" style phrasing, applied consistently across all three charts' tooltips (dropped "annual" and "CalFresh" from the wording throughout, not just the one chart, for consistency).
  - [x] "Child-only persons" renamed to "People in Child-Only Households" — checkbox label and the `DATA.demographic_layers.overlays.child_only.label` field both updated (that field also drives the tooltip text and accessible-table column header, so both follow automatically).
  - [x] Added "CalFresh is the name for the Supplemental Nutrition Assistance Program (SNAP) in California." directly under the page title.

## Phase 5 — Verification
- [ ] Spot-check pipeline output against the source Excel files (row counts, known values, date ranges)
- [ ] Confirm the dashboard renders correctly and reflects live Sheet data
- [ ] Confirm retention/pruning logic works as designed (test by simulating near-cap conditions)
- [ ] Accessibility check specifically: screen reader pass, color contrast check, keyboard-only navigation test

## Data dictionary
- [x] Cell-to-meaning mappings for CF296 (135 current-era + 123 legacy-era items) and CF18 (68 items), in `pipeline/Labels.gs`, sourced from CDSS's own header rows/DataDictionary tabs
- [x] Standalone, human-readable `DATA_DICTIONARY.md` created 2026-07-28, built from three real CDSS documents Diana pulled off the live dashboard site (`Definitions_Sources.pdf`, `Program_Reach_Index_(PRI)_Methodology_Writeup.pdf`, `Interpretation of PRI Trends.pdf`) — covers Master file column definitions, PRI methodology + interpretation caveats, Consortium/County Size/Region definitions, and the acronym glossary
- [x] Real definitions/methodology incorporated — not just column names. PRI in particular now has its full child-only-method derivation and CDSS's own stated limitations (ACS sampling noise, single-year vs five-year counties, out-of-range values) documented
- [x] Acronym glossary — all terms now confirmed directly from source documents (FPL, SOC, CFAP, SSI/SSP, MEDS, DFA256/CF256, PRI, PAI, ACS, ICT, ES, ADP, SAR7, RRR, QC, PMC, C-IV/CalWIN/LRS, and as of 2026-07-28: PACF, NACF, SNB, TNB — sourced from CDSS All County Letter 22-85, which Diana provided)
- [ ] Independent verification that the reconstructed CF296/CF18 labels in `Labels.gs` match official CDSS definitions — now much more doable: ACL 22-85's CF 296 instructions spell out every cell's item text (Cell 1 through Cell 135) and can be diffed directly against `CF296_LABELS`
- [x] ~~Resolve SNB/TNB and confirm PACF/NACF~~ — resolved 2026-07-28 via ACL 22-85 (see `DATA_DICTIONARY.md`)
- [ ] Decide on exact wording for a PRI caveat/tooltip in the eventual dashboard UI (Phase 4) — flagged in `DATA_DICTIONARY.md` as a recommendation, not yet drafted

- **CF 358F/S (race/ethnicity, sexual orientation, gender identity)** — identified 2026-07-28 via ACL 22-85 as a report we don't currently pipeline at all. Annual report (due each September 10), same CDSS reporting family as CF296/CF256. Could be a compelling addition for advocates working on equity analysis. Not scoped or built — just flagged as a candidate fourth data source.
- **CF 296 SAR 7 discontinuance edge case** — ACL 22-85E (errata, Feb 2024) clarifies that a case discontinued for a late SAR 7 counts as "carried forward" (Item 4) only if it's discontinued and reinstated within the same report month; otherwise it counts as a new case added the following month. Doesn't affect our mirroring approach (we just copy CDSS's reported cell values as-is) but worth knowing if we ever build our own month-to-month caseload reconciliation logic.

## Ideas / future considerations (not yet scoped)
- **"New Applicants" dashboard** (Diana, 2026-08-13) — once the participation view is done. Would cover application outcomes, approval rates by demographic, and the procedural-denial-rate-without-a-denominator gap Diana found in the state's own dashboard (i.e. CDSS reports a procedural denial rate somewhere without giving the denominator needed to sanity-check or recompute it — worth digging into which report/column this is and whether our pipelined data can fill the gap). Not scoped — pairs naturally with the SSI-linked disposition-flow data already sitting in `SSILinked.gs` unused, and the 116 not-yet-used columns in `Student_Table_Computed` (see Phase 4 above).
- **Audit system** — Diana flagged this as worth considering but wasn't sure yet what it should include (2026-07-17). Possible directions to discuss: automated anomaly flagging (e.g. a county's numbers dropping to near-zero, or a large month-over-month swing that might indicate a data issue vs. a real policy event), or a more formal audit trail beyond what `Pipeline_Log` already provides. Needs a real conversation before scoping.
- **Systematic spot-checking process** — beyond the ad hoc verification done during setup, a repeatable way to periodically confirm pipeline output still matches source files (manual checklist vs. some automated summary-stat/checksum comparison — TBD)
- **Request form** — a way for advocates using the public dashboard to request a new view, filter, or chart they'd find useful. Could be as simple as a linked Google Form for v1.
- Whether to also archive pruned historical data somewhere (e.g., a separate Drive file) rather than discarding it outright
- **Automate bringing in other comparison county-level data sources** (Diana, 2026-07-30) — e.g. ACS (American Community Survey) demographic/economic data, CalGrant participation data, etc., to let advocates compare CalFresh trends against other county-level indicators. Not scoped: would need source/API research per dataset, a join strategy against county names, and its own refresh cadence considerations (ACS is annual, CalGrant may differ). Flagged alongside the in-progress demographic-layers work on the Total Participants chart (see Phase 4) since both are about enriching the participant-level view with outside context.
- **Demographic toggle layers on Total CalFresh Participants chart** (Diana, 2026-07-30) — rough prototype build started as a branch copy, later consolidated into `index.html`; see Phase 4 for status.
- **Master file column-naming correction** (Diana, 2026-07-31): the Monthly tab's "Caseload Age 17 and Under / 18 to 49 / 50 and Over" (and the parallel Gender/Race/Ethnicity "Caseload" columns) are misleadingly named — they're actually **students on CalFresh**, not the general caseload (confirmed: they sum to the same total as `Caseload Total Student Count`, a ~200K statewide figure, versus ~5.2M total CalFresh persons). The real age breakdown of the total caseload lives in the **Annual** tab instead: `Elderly CalFresh July` / `Age 18 to 59 CalFresh July` / `Children CalFresh July`, which sum to within ~1–3% of the actual total CalFresh persons count each July. Worth revisiting whether the Caseload Gender/Race/Ethnicity columns are similarly mislabeled (likely also students-only, not general-caseload) before using them anywhere.

- **SSI-linked persons — attempted 2026-08-12, participant-count approach abandoned; disposition-flow data kept for future Applications Outcomes use.** Original plan: replace the placeholder `ssi_linked` overlay on the Total CalFresh Participants chart with a real "People in SSI-Linked Households" participant total, computed server-side in the pipeline so it self-refreshes with no manual step. Implemented in `SSILinked.gs`, writing to a new `SSI_Linked_Computed` tab, called from `PIPELINE_MAIN()` in `Main.gs` right after the weekly Master ingest (so it recomputes every Monday with no manual step) — this refresh architecture worked and is being kept.
  - **Column-meaning correction, 2026-08-12 (the actual reason this got abandoned):** the plan assumed the 9 ` SSI Person Race/Ethnicity - [X]` columns in `Master_Monthly` were a standing count of everyone currently SSI-linked on CalFresh ("All SSI Recipients"). They're not — Diana caught that the total came out nowhere near the ~700K statewide SSI population. Their real meaning, confirmed by their position in the column list (immediately after the `SSI Disposed HH Language - [X]` columns and right before the SNB section starts, in a cluster with `New Apps with at Least Some SSI - Approved/Denied`, `SSI Persons in New Apps Disposed`, and `Average Age of SSI Persons Newly Applying`) and confirmed by Diana directly summing them against `SSI Persons in New Apps Disposed`: they're the race/ethnicity breakdown of that same column — a **monthly flow** of SSI-linked persons whose CalFresh application was approved or denied that month, not a running stock. There is no standing "everyone SSI-linked on CalFresh" total anywhere in `Master_Monthly` today, so the "People in SSI-Linked Households" participant-count feature isn't buildable with current data — **dropped from the Total CalFresh Participants chart's Participant Types panel.** The old placeholder `ssi_linked` overlay in the chart itself was never touched during this attempt (we were still waiting on real numbers when the mismatch was caught), so nothing needs undoing there.
  - **Kept, relabeled to match reality:** `SSILinked.gs` still computes and cross-checks these figures (useful for a future Applications Outcomes dashboard comparing SSI-linked application volume/outcomes/demographics against the equivalent all-applicant CF296 figures), just under accurate names now:
    - `SSI Persons in New Apps Disposed (direct)` = the `SSI Persons in New Apps Disposed` column, as-is (a monthly flow, not a stock — don't treat as a participant count).
    - `SSI Persons in New Apps Disposed (race sum, cross-check)` = sum of the 9 ` SSI Person Race/Ethnicity - [X]` columns, cross-checked against the direct total above (confirmed by Diana to match).
    - `SNB SSI Persons (direct)` = `Total SSI Persons in SNB Households`, cross-checked against summing the 9 `SNB SSI Person Race [X]` columns. This one **is** a real standing stock figure (current SNB household membership) — don't confuse it with the disposed-applications flow above.
    - `TNB SSI Persons (direct)` = `Total SSI  Persons in TNB Households` (note: real header cell has a doubled space between "SSI" and "Persons" — easy to miss, caused an earlier pass to wrongly assume no direct TNB total existed), cross-checked against summing the 9 `TNB SSI Person Race [X]` columns. Also a real standing stock figure, like SNB's.
    - `Total SNB/TNB Households` and `SNB/TNB Avg Household Size (Aided Only)` kept as raw reference columns.
  - Two real naming quirks found and handled generically rather than hardcoded per-instance: (a) scattered inconsistent whitespace throughout `Master_Monthly`'s header row (leading/trailing/doubled spaces with no visible pattern) — handled via whitespace normalization in `resolveColumns_()`, not by matching exact quirky strings; (b) CDSS's SNB/TNB-specific race columns use shorter labels for two of the nine categories than the general columns do ("Native Hawain or Pacific Islander" instead of "Native Hawaiian or Other Pacific Islander", and "More Than One Race" instead of "More Than One Race/Ethnicity") — handled via the separate `SSI_RACE_CATEGORIES_SNB_TNB` list in `SSILinked.gs`.
  - **Data-quality bug found 2026-08-12, affects all of `Master_Monthly`, not just this feature:** the `Date` column is silently corrupted sheet-wide. CDSS's source stores it as text like "Jan-14" (meaning January 2014); the xlsx-to-Sheets conversion in `IngestMaster.gs` auto-parses that as day-month with no year given, so "14" becomes the day-of-month and the year defaults to whenever the conversion last ran (e.g. 2026) instead of the real 2014 — confirmed directly against a live cell (displays "Jan-14," but the formula bar shows the real value is `1/14/2026`). `SSILinked.gs` works around this by building its own "YYYY-MM" period from the `Month` (text) and `Calendar Year` (number) columns instead, both of which are unaffected. Anything else that reads `Master_Monthly`'s `Date` column directly has the same bug — worth a proper fix in `IngestMaster.gs` itself later (e.g. force that column to import as literal text, or reconstruct it from Month + Calendar Year at ingest time), not just worked around per-consumer.
  - **Future Applications Outcomes dashboard**: this whole disposed-SSI-applications cluster (`New Apps with at Least Some SSI - Approved/Denied`, `SSI Persons in New Apps Disposed` + its race/ethnicity and household-language breakdowns, `Average Age of SSI Persons Newly Applying`) is real, useful flow data for comparing SSI-linked application outcomes against the general applicant pool once that dashboard gets scoped — not wasted effort, just not usable for a participant-count chart.
  - Since the participant-count feature is dropped, the earlier open question ("which of these become their own Participant Types checkboxes") is moot. **Closed out 2026-08-13:** the `ssi_linked` checkbox, its `activeOverlays`/`layerCheckboxes` wiring, and its `DATA.demographic_layers.overlays` entry were all removed from `index.html` (Diana: "remove SSI-linked persons from the options") rather than left as a placeholder. Can be re-added the same way `students` was if a real standing-total data source ever turns up.
  - **Live-fetch architecture (built 2026-08-12, worth keeping regardless of this feature's fate):** since the whole spreadsheet is already published-to-web with auto-republish (`SETUP.md` step 8, confirmed working), any new computed tab like `SSI_Linked_Computed` becomes fetchable immediately via the same `gviz/tq?tqx=out:csv&sheet=NAME` pattern already planned for the Phase 4 live-fetch swap (see that item above) — no new publishing step, no new hosting piece. A small CSV-parsing step (e.g. PapaParse from CDN) will still be needed client-side once a chart switches from its embedded JSON snapshot to this live fetch.

- **Students on CalFresh — started narrow 2026-08-13, expanded to a full reference table same day** (Diana). Started as a single-column pull (`Caseload Total Student Count`) via a new `CaseloadStudents.gs`, matching the `SSILinked.gs` self-refresh pattern. Diana then asked to expand it into a complete ~117-column student table (same file, same tab name changed to `Student_Table_Computed`), covering the "Applications ... At Least One Student" / "Denial Reason" / "New Applications ..." / "Caseload ..." column range through to the end of `Master_Monthly`.
  - **Script-proliferation check-in, 2026-08-13 (worth remembering as a general principle):** Diana pushed back on why a new script was needed here when age/child-only/Medi-Cal data made it into the chart earlier without one — fair question. The honest answer: those earlier categories were pulled into the chart in an earlier session not fully visible in this history, so it's unclear whether that was actually simpler or just used tools that worked better at the time. What's true this session: direct bulk reads of `Master_Monthly` (~300 columns) through the available Drive-connector tools have been unreliable (truncated dumps, wrong-tab CSV exports, empty live-fetch responses) — that's the real reason a script was reached for, not a hard technical requirement in every case. SSI genuinely needed one (real computation, cross-checks). A single existing column like `Caseload Total Student Count` didn't, strictly — a `QUERY()` formula in a plain Sheets tab would have worked with no Apps Script at all. General takeaway for future asks: default to the simplest thing that gets the numbers out (a formula, a manual export) and only reach for a new `.gs` file when real computation or self-refresh is actually needed, not just because a script pattern is fresh in mind.
  - **Working theory, not yet confirmed:** the entire column range requested (spanning both "New Applications ..." and "Caseload ..." blocks, including many columns with no "student" in their own name) may be entirely student-scoped, consistent with the earlier finding that "Caseload Age" was students-only despite its generic name (see the "Master file column-naming correction" entry above — those columns summed to this same `Caseload Total Student Count` total). If confirmed, "New Applications Age 17 and Under" etc. would mean "new applications with at least one student," not all new applications — worth verifying (e.g. does `New Applications Age Total` roughly match the "at least one student" application-approval/denial/pended counts?) before using any of this range as if it covers the general applicant/caseload population.
  - Column-name quirks found and handled generically (not hardcoded per instance): scattered whitespace, PLUS inconsistent hyphen-spacing within the same "Exemptions-..." block (e.g. "Exemptions- Care" vs "Exemptions-Care") — `normalizeHeaderText_()` in `SSILinked.gs` (shared across all `.gs` files) now strips whitespace immediately touching hyphens in addition to collapsing whitespace runs, so both variants compare equal.
  - Resolution is deliberately partial for this table (unlike `SSILinked.gs`'s all-or-nothing): County/Month/Calendar Year are structural and still hard-fail if missing, but the ~117 data columns resolve independently — whatever's found gets written, and anything not found is logged by name so mismatches (likely, given the column count was hand-transcribed) are easy to fix one at a time rather than blocking the whole table.
  - Cell values are copied as-is (no numeric coercion), unlike `SSILinked.gs`'s sums — this preserves CDSS's "*" small-cell suppression marker rather than silently zeroing it, which matters more here since these are much finer-grained slices (single language/county/month) more likely to hit the <11-person threshold.
  - **Live-run confirmed 2026-08-13:** 8,909 rows written, 117 of 117 requested data columns resolved on the first pass (no transcription fixes needed), 0 rows skipped for an unrecognized Month/Calendar Year combination. `*` suppression markers are showing up in the raw output as expected, more often than in `SSILinked.gs`'s totals since these are finer-grained slices.
  - **Wired into the chart, 2026-08-13:** of the 117 columns, only `Caseload Total Student Count` was wanted on the Total CalFresh Participants chart (the rest stay reserved for a future Applications Outcomes / Demographics view). Added as a new "Students" overlay in `index.html`'s Participant Types panel — same pattern as child-only/SSI-linked/dual-Medi-Cal (checkbox, `activeOverlays`, `layerCheckboxes`, `DATA.demographic_layers.overlays.students`). Data source: Diana downloaded `Student_Table_Computed` as a CSV and uploaded it directly (the `docs.google.com` gviz endpoint is unreachable through this session's web-fetch tool — confirmed again by testing it live against a known-working control URL — and there's no dedicated Google Sheets connector available to add; Claude in Chrome would fix this properly but wasn't connected this session). Notable in the real numbers: reporting starts 2023-01 (nothing before, statewide and per-county), runs monthly with no gaps through 2026-05 (two months behind the file's general data lag), and Mono County has no non-suppressed reading in any month (blank, not CDSS's `*` marker — worth a closer look if Mono ever needs to be highlighted specifically). Also added a defensive `if (!layer) return` guard in the overlay-rendering loop so a checkbox wired in ahead of its data (as students briefly was) degrades gracefully instead of throwing.
  - Still open: test the "this whole 117-column range is student-scoped" theory above (e.g. compare `New Applications Age Total` to the "at least one student" application counts) before treating any of the other 116 columns as usable for a general-population view.

- **Feb 2019 data gap — found and worked around 2026-08-13** (Diana): nearly every county's `CalFresh Persons` figure drops sharply for exactly one month (Feb 2019) then fully rebounds the next month — e.g. Sacramento goes 187,356 (Jan) → a Feb reading way below trend → 187,629 (Mar). Checked programmatically against the embedded `DATA.series[county].persons` in `index.html` rather than eyeballing it: most counties plus the Statewide total show an 80–89% single-month drop relative to the Jan/Mar average, with a handful (Placer, Fresno, Shasta) showing little or no drop. **Decided 2026-08-13 (Diana): treat every county the same rather than drawing an exceptions list** — the pattern is widespread enough that singling out a few counties as "clean" wasn't worth the added complexity or the footer-note explaining, and skipping one historical month everywhere causes no real problem. Feb 2019 is now nulled for all 58 counties plus Statewide, no exceptions.
  - **Sonoma has a second, independent bad month**: Apr 2019 shows the same drop-and-rebound pattern (persons: 24,175 → 3,514 → 24,955), confirmed isolated to Sonoma alone (every other county's Apr 2019 reading is within a few percent of normal) — nulled for Sonoma only.
  - **Fix applied in the embedded DATA blob** (not just visually masked): for every affected county-month, set `persons[month] = null` AND `yoy_pct[month] = null` AND `yoy_pct[month + 1 year] = null` — the year-over-year field is a precomputed static value in this snapshot (not computed live from `persons` in the browser), so a bad Feb 2019 reading was also corrupting Feb 2020's YoY% (e.g. Sacramento's Feb 2020 YoY was showing +763.5%, an artifact of dividing by the bad Feb 2019 base) — both legs needed nulling, not just the bad month itself. With `spanGaps: false` already set on every line dataset, a null value now renders as an honest gap in the trend line instead of a fake cliff.
  - Added a footer note (`<footer class="source-note">`) documenting this, in the same place as the other data-caveat notes.
  - Production-swap reminder: once Phase 4's live-fetch swap happens (replacing the embedded JSON snapshot with a live `gviz` fetch from the Sheet), this null-out logic needs to move somewhere durable — either as a small client-side exclusion list keyed by (county, month) checked at render time, or as a correction applied server-side in the pipeline — otherwise this fix disappears the moment the snapshot is regenerated from a fresh pull.
